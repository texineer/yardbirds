# BleacherBox Configuration

# Server port
PORT=3001

# Database path (optional, defaults to data/bleacherbox.db)
# DB_PATH=/opt/bleacherbox/data/bleacherbox.db

# Year for scraping (optional, defaults to current year)
# YEAR=2026

# Session secret for auth (REQUIRED in production — server refuses to start without it)
# Generate one with:  openssl rand -hex 32
SESSION_SECRET=change-me-to-a-random-string

# Comma-separated allowlist of front-end origins permitted to make credentialed
# CORS requests in production. Leave unset in development (localhost is allowed).
# ALLOWED_ORIGINS=https://bleacherbox.app,https://www.bleacherbox.app

# Global admin bootstrap. Set GLOBAL_ADMIN_PASSWORD once to seed the initial
# admin account; the password is only used on first run when the account does
# not yet exist. Optionally override the login/contact emails.
# GLOBAL_ADMIN_PASSWORD=set-a-strong-password
# GLOBAL_ADMIN_EMAIL=ray@bleacherbox.app
# GLOBAL_ADMIN_CONTACT_EMAIL=ray@texineer.com

# ElevenLabs PA Announcer voice (optional — enables ballpark-quality player announcements)
# Sign up free at https://elevenlabs.io — free tier covers a full team many times over
# ELEVENLABS_API_KEY=your-api-key-here
# ELEVENLABS_VOICE_ID=pNInz6obpgDQGcFmaJgB  # "Adam" — deep, authoritative (default)

# Teams are now configured in the database via POST /api/teams
# No per-team env vars needed — add teams via the API:
#
# curl -X POST http://localhost:3001/api/teams \
#   -H "Content-Type: application/json" \
#   -d '{"slug":"yardbirds","pgOrgId":50903,"pgTeamId":276649,"name":"RR Yardbirds 14U","ageGroup":"14U","ftTeamUuid":"cc705482-...","ftSeasons":"2026-baseball"}'
